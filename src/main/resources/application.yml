spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lookup_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        show_sql: true
    database: postgresql

redis:
  host: localhost
  port: 6379
